# Copy this file to `.env` and fill in your Binance Futures Testnet API credentials.
# Generate keys at https://testnet.binancefuture.com (log in -> API Key).
BINANCE_API_KEY=your_testnet_api_key_here
BINANCE_API_SECRET=your_testnet_api_secret_here

# Optional. Only change this if Binance migrates the testnet host again
# (e.g. to https://demo-fapi.binance.com). Defaults to the URL in the task spec.
# BINANCE_BASE_URL=https://testnet.binancefuture.com
